# PRD: AI 학습 여부 판단 모델

## 1. 개요

### 목적

실험자가 현재 사용 중인 탭/창의 텍스트 내용과 업로드된 학습 자료를 비교 분석하여 **공부 중(STUDYING) / 공부하지 않음(NOT_STUDYING)** 을 자동으로 판단하는 AI 모델을 정의한다.

### 기존 모델과의 차이

| 항목 | 기존 (MobileNetV2) | 신규 (텍스트 기반 LLM) |
|------|-------------------|----------------------|
| 입력 | 화면 스크린샷 이미지 | 현재 창 텍스트 + 학습 자료 텍스트 |
| 판단 방식 | 시각적 패턴 분류 | 의미적 관련성 분석 |
| 학습 데이터 | 수동 캡처 이미지 필요 | 학습 자료 업로드만으로 즉시 적용 |
| 설명 가능성 | 없음 | 판단 근거 텍스트 제공 |

---

## 2. 입력 데이터 명세

### 2.1 학습 자료 (기준 데이터)

| 항목 | 설명 | 비고 |
|------|------|------|
| 업로드된 원본 자료 | 실험자가 학습해야 하는 PDF/문서 | 세션 시작 전 업로드 |
| 생성된 학습자료 | 원본 자료 기반으로 앱이 생성한 요약/퀴즈 | 실험 웹사이트 내부 |
| 핵심 키워드 | 자료에서 추출한 과목 특화 개념어 목록 | 사전 추출하여 캐싱 |
| 주제 범위 | 학습해야 하는 개념의 전체 범위 | 관련성 판단 경계 설정 |

### 2.2 현재 활성 창 데이터 (판단 대상)

우선순위 순으로 수집:

| 우선순위 | 데이터 | 수집 방법 |
|---------|--------|-----------|
| 1 | 현재 탭 제목 (`document.title`) | Page Visibility API |
| 2 | 검색어 또는 입력 중인 텍스트 | `<input>`, `<textarea>` 값 |
| 3 | 사용자가 선택한 텍스트 | `window.getSelection()` |
| 4 | 클릭/스크롤이 발생한 영역의 텍스트 | 이벤트 리스너 + DOM 쿼리 |
| 5 | 화면 중앙 본문 텍스트 | viewport 중앙 element 텍스트 |
| 6 | URL 및 도메인 | `location.href` |
| 7 | 페이지 전체 텍스트 | `document.body.innerText` (보조) |

> **주의:** 페이지 전체 텍스트(우선순위 7)는 광고, 댓글, 메뉴, 추천 콘텐츠 등 노이즈가 많으므로 보조 정보로만 사용한다.

### 2.3 상호작용 신호 (보조 데이터)

| 신호 | 활용 방식 |
|------|-----------|
| 클릭 영역 | 사용자가 실제로 관심을 둔 콘텐츠 영역 추정 |
| 스크롤 위치 | 현재 읽고 있는 본문 영역 추정 |
| 마우스 이동 경로 | 관심 영역 보조 추정 (단독 판단 기준 아님) |
| 텍스트 선택 범위 | 집중 독서 영역 직접 확인 |

---

## 3. 판단 로직

### Step 1. 현재 활성 탭/창 유형 분류

```
현재 창이 → 실험 웹사이트       → STUDYING (추가 분석 없이 즉시)
           → 허용된 AI 창       → STUDYING (추가 분석 없이 즉시)
           → 기타 사이트/앱     → Step 2로 진행
```

**허용된 AI 창 목록 (MVP):**
- `chat.openai.com`, `claude.ai`, `gemini.google.com`, `perplexity.ai` 등 LLM 서비스
- 추후 관리자 설정으로 확장 가능

### Step 2. 페이지 내부 상호작용 확인

현재 창 내에서 최근 3초 이내 발생한 이벤트(클릭, 스크롤, 텍스트 선택, 입력)의 영역을 파악한다. 이 신호는 사용자가 실제로 관심을 두는 콘텐츠 영역을 추정하는 데 사용된다.

### Step 3. 분석 텍스트 구성

우선순위에 따라 현재 창의 텍스트를 조합한다:

```
분석 텍스트 = [탭 제목] + [검색어/입력값] + [선택 텍스트] 
            + [상호작용 영역 텍스트] + [viewport 본문]
```

토큰 한도 초과 시: 우선순위 높은 항목부터 포함, 낮은 항목은 요약 또는 생략.

### Step 4. 학습 자료 기준과 비교 (핵심 판단)

AI는 다음 기준으로 STUDYING / NOT_STUDYING을 판단한다:

**STUDYING으로 분류하는 경우:**
- 현재 창 텍스트가 학습 자료의 핵심 개념과 **동일한 의미적 맥락**으로 사용됨
- 현재 창의 사용 목적이 학습 자료 이해, 검색, 요약, 번역, 문제 풀이, 관련 자료 확인에 해당
- 검색어가 학습 주제와 직접 연관됨 (예: 학습 주제 "수요예측" → 검색어 "계절성 수요예측 방법")

**NOT_STUDYING으로 분류하는 경우:**
- 현재 창이 SNS, 쇼핑, 게임, 오락성 콘텐츠 등 학습 목적과 거리가 멈
- 현재 창 텍스트와 학습 자료 간 의미적 관련성이 낮음
- 학습 관련 단어가 등장해도 다른 문맥으로 사용됨 (단순 키워드 일치 ≠ 관련성)

**오판 방지 원칙:**
- 단순 키워드 일치가 아닌 **문맥적 유사도**를 우선한다
- "AI", "모델", "서비스" 같은 범용 단어는 과목 특화 개념어보다 낮은 가중치를 부여한다
- 광고, 댓글, 추천 콘텐츠 영역의 텍스트는 낮은 가중치로 처리한다

### Step 5. 타이머 제어 신호 출력

```
STUDYING    → 타이머 RESUME 또는 유지
NOT_STUDYING → 타이머 PAUSE
```

---

## 4. 출력 형식

```json
{
  "state": "STUDYING" | "NOT_STUDYING",
  "confidence": 0.0 ~ 1.0,
  "timer_action": "RESUME" | "PAUSE",
  "reason": "판단 근거 (1~2문장, 한국어)",
  "active_window_type": "experiment_site" | "allowed_ai" | "other",
  "analyzed_text_summary": "분석에 사용된 텍스트 핵심 요약 (최대 100자)",
  "matched_concepts": ["학습 자료와 일치한 핵심 개념어 목록"]
}
```

**출력 예시 (STUDYING):**
```json
{
  "state": "STUDYING",
  "confidence": 0.87,
  "timer_action": "RESUME",
  "reason": "현재 검색어와 본문이 학습 자료의 핵심 개념인 '수요 예측', '계절성', '지수평활법'과 관련되어 있음",
  "active_window_type": "other",
  "analyzed_text_summary": "계절성 수요예측 방법 - 시계열 분해와 지수평활",
  "matched_concepts": ["수요예측", "계절성", "지수평활법"]
}
```

**출력 예시 (NOT_STUDYING):**
```json
{
  "state": "NOT_STUDYING",
  "confidence": 0.92,
  "timer_action": "PAUSE",
  "reason": "현재 창은 SNS 피드로, 학습 자료의 주제와 관련성이 없음",
  "active_window_type": "other",
  "analyzed_text_summary": "인스타그램 피드 - 패션, 여행 콘텐츠",
  "matched_concepts": []
}
```

---

## 5. 구현 방식

### 5.1 LLM API 기반 판단

| 항목 | 내용 |
|------|------|
| 모델 | `gpt-4o-mini` (빠른 응답, 낮은 비용) |
| 호출 방식 | 서버리스 API 엔드포인트 (클라이언트에서 직접 호출 금지) |
| 프롬프트 구조 | System: 학습 자료 기준 + 판단 규칙 / User: 현재 창 텍스트 |
| 응답 형식 | JSON (`response_format: { type: "json_object" }`) |
| 최대 토큰 | Input: 2,000 / Output: 300 |

### 5.2 학습 자료 기준 생성 (세션 시작 시 1회)

세션 시작 시 학습 자료를 전처리하여 시스템 프롬프트에 포함할 기준을 생성한다:

```
1. 핵심 개념어 추출 (최대 20개)
2. 주제 범위 요약 (3~5문장)
3. 관련 검색어/키워드 예시 목록
4. 학습 무관 판단 예시 (오판 방지용 네거티브 예시)
```

이 기준은 세션 내내 재사용되므로 **System 프롬프트 고정** + OpenAI의 `max_tokens` 조정으로 비용을 최소화한다.

### 5.3 프롬프트 구조

```
[System Prompt - 세션 시작 시 고정]
당신은 학습 여부를 판단하는 AI입니다.

학습 자료 정보:
- 주제: {학습 주제}
- 핵심 개념: {추출된 개념어 목록}
- 주제 범위 요약: {요약}

판단 규칙:
- 현재 창 텍스트가 위 핵심 개념과 같은 의미 맥락이면 STUDYING
- SNS, 쇼핑, 게임, 오락성 콘텐츠면 NOT_STUDYING
- 단순 키워드 일치가 아닌 문맥적 관련성으로 판단
- 응답은 반드시 JSON 형식으로만 출력

[User Prompt - 매 판단 시]
현재 활성 창 정보:
- 탭 제목: {title}
- URL: {url}
- 검색어/입력값: {input_text}
- 선택된 텍스트: {selected_text}
- 본문 텍스트 (상위 500자): {body_text}

위 정보를 바탕으로 공부 여부를 JSON으로 판단해주세요.
```

### 5.4 호출 타이밍

| 조건 | 동작 |
|------|------|
| 실험 웹사이트 / 허용된 AI 창 활성 | API 호출 없이 즉시 STUDYING |
| 기타 창으로 전환 후 **3초 경과** | API 판단 호출 실행 |
| 3초 미만 순간 전환 | 이전 상태 유지 (API 호출 없음) |
| 판단 결과 수신 후 | 타이머 제어 실행 |

### 5.5 재판단 주기

- 기타 창에 머무는 동안 **30초마다** 재판단 실행
- 탭 제목 또는 URL이 변경되면 즉시 재판단

---

## 6. 오판 방지 메커니즘

### 6.1 텍스트 우선순위 가중치

| 영역 | 가중치 |
|------|--------|
| 탭 제목, 검색어, 입력값 | 높음 (3x) |
| 선택 텍스트, 클릭 영역 | 높음 (3x) |
| viewport 본문 | 보통 (1x) |
| 광고, 댓글, 사이드바, 메뉴 | 낮음 (0.2x) |
| 페이지 전체 텍스트 | 보조 (0.1x) |

### 6.2 키워드 문맥 검증

단순 키워드 출현이 아닌, 학습 자료와 **동일한 의미 맥락**에서 사용되는지 확인:
- "모델" → 패션 모델 vs 예측 모델 구분
- "AI" → AI 챗봇 업무 vs AI 기술 학습 내용 구분

### 6.3 도메인 분류 보조 참조 (MVP 이후)

| 도메인 유형 | 기본 분류 |
|------------|-----------|
| 학술 검색 (scholar.google.com, dbpia.co.kr 등) | STUDYING 경향 |
| SNS (instagram.com, twitter.com 등) | NOT_STUDYING 경향 |
| 쇼핑 (coupang.com, gmarket.co.kr 등) | NOT_STUDYING 경향 |
| 동영상 플랫폼 (youtube.com) | 제목/내용으로 재판단 필요 |

도메인 분류는 **참고 정보**로만 사용하며 최종 판단은 항상 텍스트 관련성에 따른다.

---

## 7. 로그 설계

판단마다 다음 항목을 기록한다:

| 항목 | 타입 | 설명 |
|------|------|------|
| `timestamp` | ISO 8601 | 판단 시점 |
| `active_window_type` | string | `experiment_site` / `allowed_ai` / `other` |
| `active_domain` | string | 현재 도메인 또는 앱명 |
| `interaction_area` | string | 최근 상호작용 영역 설명 |
| `analyzed_text_summary` | string | 분석 텍스트 핵심 요약 (원문 미저장) |
| `ai_state` | string | `STUDYING` / `NOT_STUDYING` |
| `confidence` | number | 판단 신뢰도 (0~1) |
| `timer_action` | string | `RESUME` / `PAUSE` |
| `reason` | string | AI 판단 근거 |
| `matched_concepts` | string[] | 일치한 핵심 개념어 목록 |
| `previous_state` | string | 직전 상태 |
| `state_duration_ms` | number | 해당 상태 지속 시간 |

> **개인정보 보호:** 원문 텍스트 전체는 저장하지 않고 요약, 키워드, 판단 결과, 근거만 저장한다.

---

## 8. 성능 요구사항

| 지표 | 목표값 |
|------|--------|
| 판단 지연 시간 | 창 전환 후 3초 이내 판단 완료 |
| API 호출 응답 시간 | 2초 이내 |
| 판단 정확도 (실제 공부/안공부 일치율) | 85% 이상 |
| False Pause Rate (공부 중인데 멈춤) | 10% 이하 |
| False Resume Rate (안공부인데 작동) | 15% 이하 |
| 타이머 과잉 재개/멈춤 빈도 | 1분당 2회 이하 |

---

## 9. MVP 범위

### 포함

- 현재 활성 탭/창 유형 자동 분류 (실험 웹사이트 / 허용 AI / 기타)
- 학습 자료 기반 시스템 프롬프트 자동 생성 (세션 시작 시)
- 현재 창 텍스트 수집 및 우선순위 기반 조합
- OpenAI API(`gpt-4o-mini`) 호출을 통한 STUDYING / NOT_STUDYING 이진 판단
- 판단 근거 텍스트 생성
- 3초 유예 시간 적용
- AI 판단 로그 저장

### 제외 (향후 개선)

- 허용 AI 창 내 대화 내용의 정밀 분석
- 도메인 화이트리스트/블랙리스트 관리 UI
- 웹캠 시선 추적 연동
- 키보드/마우스 행동 패턴 집중도 분석
- 운영체제 전체 마우스 위치 추적

---

## 10. 기술 스택

| 항목 | 선택 | 이유 |
|------|------|------|
| LLM | `gpt-4o-mini` | 빠른 응답(< 1s), 낮은 비용, JSON 출력 안정적 |
| 텍스트 수집 | 화면 공유 스트림 + DOM 텍스트 추출 | 기존 캡처 인프라 재활용 가능 |
| API 게이트웨이 | Vercel Edge Function | 기존 배포 환경 활용, API 키 서버 측 보호 |
| 응답 형식 | `response_format: { type: "json_object" }` | 파싱 안정성 보장 |
| 로그 저장 | 기존 `localStorage` + 세션 로그 구조 재사용 | `SessionEvent` 타입 확장 |
